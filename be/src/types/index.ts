export enum Order {
    DESC = "DESC",
    ASC = "ASC",
}

export enum SortBy {
    CREATED_AT = "created_at",
    UPDATED_AT = "updated_at",
}

export enum Role {
    ADMIN = "Admin",
    WRITER = "Writer",
    USER = "User",
}
